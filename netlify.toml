# Consumer mini-app = site root (/). Admin = /admin.html (same dist from root Vite build).
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200
